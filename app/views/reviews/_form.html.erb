<%= simple_form_for [menu, review],
  data: {review_target: "form", action: "submit->review#send"} do |f| %>
  <%= f.input :content %>
  <%= f.submit "Add a Review!", class:"btn btn-primary"%>
<% end %>
